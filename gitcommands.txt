commands

	git clone path
	
	git command -h ->to find parameters vs.
	
	git commit ->allow us to create a commit
	
	git log ->gives us an overview of commits
	
	git add . -> add one or more files for staging
	git add file1.txt
	
	git reset <filename> ->removes one or mor files from staging
	